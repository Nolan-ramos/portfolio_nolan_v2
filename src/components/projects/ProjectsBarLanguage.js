import React from 'react';

const ProjectsBarLanguage = ({ language }) => {
    return (
        <div className='projects__bar__collapse__languages__language'>
            {language}
        </div>
    );
};

export default ProjectsBarLanguage;